// you cannot use get side function in a component unless its a page
// so you have to do is first in index.js  <Product product ={}props.products[0]; />
// and use props in Product compomnent with props.product.name

export default function Product({}) {
  return <></>;
}
